div(click-login, escapable="true")
div
	form
		.formTitle Boards
		label(ng-show='showWrite') Write Access
		div(ng-repeat='board in boards.write')
			label {{board.name}}
			button(ng-click='editClick(board._id)').btn.btn-default Edit
		br
		label(ng-show='showRead') Read Access
		div(ng-repeat='board in boards.read')
			label {{board.name}}